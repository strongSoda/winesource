import { motion } from 'framer-motion';
import styled from 'styled-components';

const OrderSuccessWrapper = styled.div`
    .content {
        margin-top: 12vh;
        /* background-color: blue; */
            text-align: center;
        h1 {
        }

        img {
            height: 30vh;
        }
    }
`;

export { OrderSuccessWrapper };
